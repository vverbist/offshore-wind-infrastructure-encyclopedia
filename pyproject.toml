[project]
name = "offshore-wind-infrastructure-encyclopedia"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "coolprop",
    "matplotlib",
    "numpy",
    "openpyxl>=3.1.5",
    "pandas",
]

[tool.uv]
package = false
