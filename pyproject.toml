[project]
name = "offshore-wind-infrastructure-encyclopedia"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "coolprop",
    "matplotlib",
    "numpy",
    "pandas",
]

[tool.uv]
package = false
